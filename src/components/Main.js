import { Stack } from '@chakra-ui/core'

export const Main = (props) => (
  <Stack
    spacing="1.5rem"
    width="100%"
    maxWidth="52rem"
    // mt="-45vh"
    pt="2rem"
    px="1rem"
    {...props}
  />
)
